import { EmailenDirective } from './emailen.directive';

describe('EmailenDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailenDirective();
    expect(directive).toBeTruthy();
  });
});
